apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - rbac.yaml
  # clustersecretstore/ is NOT listed here — it requires ESO CRDs which are
  # installed by the HelmRelease above. Apply it after the HelmRelease is ready:
  #   make platform-openbao-up  (handles the ordering automatically)
